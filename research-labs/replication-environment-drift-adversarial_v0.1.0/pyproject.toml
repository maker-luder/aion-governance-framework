[project]
name = "replication-environment-drift-adversarial"
version = "0.1.0"
description = "Research-only replication environment drift and interpretation contract"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
