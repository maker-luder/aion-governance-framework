# Real-Model Candidate — Public-Safe Research Surface

This directory contains public-safe PyTorch model source, training/evaluation scripts, evidence summaries, and registries from the local AION/Astra real-model completion workspace.

It does **not** contain checkpoint binaries or dataset binaries. The public registries retain hashes, provenance, license/PII assessments and retrieval instructions without fabricating current GitHub artifact presence. Checkpoint publication remains an Owner-review decision.

Eight local learned checkpoints were validated in the separate local handoff bundle: seven `TRAINED_FROM_SCRATCH` and one `PEFT_ADAPTED` local scratch-base adapter. `G1-BASE-QWEN3-4B-INSTRUCT-2507` remains `RESOURCE_BLOCKED`; the local adapter is not the formal G1-Qwen adapter.

```text
CANONICAL_EFFECT = NONE
DEPLOYMENT = FALSE
INDEPENDENT_IVV = NOT_ACHIEVED
SUBJECTIVITY_CONCLUSION = NOT_ESTABLISHED
IDENTITY_CONCLUSION = NOT_ESTABLISHED
```

All learned outputs are advisory. They cannot authorize operations, canonize or rewrite provenance, cross namespace/supersession/deletion/recovery/audit gates, or establish identity, subjectivity or phenomenal status.
