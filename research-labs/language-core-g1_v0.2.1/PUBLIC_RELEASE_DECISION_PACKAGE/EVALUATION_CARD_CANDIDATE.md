# Evaluation Card Candidate

The framework covers 25 baseline categories and 22 side-effect dimensions. Formal model metrics are absent because the fixed-revision model was not loaded on the current host. Preview-only outputs are excluded from baseline evidence.
