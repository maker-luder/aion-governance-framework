[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-power-analysis-uncertainty"
version = "0.1.0"
description = "Research-only assumption-dependent power planning and uncertainty contract"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
