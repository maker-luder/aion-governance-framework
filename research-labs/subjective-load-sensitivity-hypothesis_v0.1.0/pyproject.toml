[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-slsh-research-method"
version = "0.1.0"
description = "AION Subjective Load Sensitivity Hypothesis research-method artifacts"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
