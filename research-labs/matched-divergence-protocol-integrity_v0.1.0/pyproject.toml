[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-matched-divergence-protocol-integrity"
version = "0.1.0"
description = "Research-only matched-divergence comparison protocol integrity contract"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
