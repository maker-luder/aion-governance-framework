[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-factorial-completeness-contract"
version = "0.1.0"
description = "Research-only full-factorial matrix completeness contract prototype"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
