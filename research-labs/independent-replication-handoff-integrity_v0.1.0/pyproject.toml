[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-independent-replication-handoff-integrity"
version = "0.1.0"
description = "Research-only independent replication handoff artifact integrity contract"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
