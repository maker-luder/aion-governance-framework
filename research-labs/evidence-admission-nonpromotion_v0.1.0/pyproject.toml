[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-evidence-admission-nonpromotion"
version = "0.1.0"
description = "Research-only evidence admission and non-promotion contract"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
