[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-preregistered-intervention-integrity-adversarial"
version = "0.1.0"
description = "Research-only adversarial audit for preregistration and intervention integrity"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
