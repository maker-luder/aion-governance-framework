[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-trace-provenance-crosswalk-adversarial"
version = "0.1.0"
description = "Research-only adversarial audit for trace provenance crosswalks"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
