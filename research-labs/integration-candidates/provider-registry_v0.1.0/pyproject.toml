[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-provider-registry-candidate"
version = "0.1.0"
description = "Offline provider metadata and deterministic selection candidate"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
