[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-tokenizer-contract-candidate"
version = "0.1.0"
description = "Offline deterministic tokenizer contract candidate"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
