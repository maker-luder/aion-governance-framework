[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-research-integrity-security-adversarial"
version = "0.1.0"
description = "Research-only adversarial audit for research integrity evidence gates"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
