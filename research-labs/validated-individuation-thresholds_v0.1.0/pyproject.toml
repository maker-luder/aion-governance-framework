[project]
name = "validated-individuation-thresholds"
version = "0.1.0"
description = "Research-only audit contract for preregistered individuation criterion profiles"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
