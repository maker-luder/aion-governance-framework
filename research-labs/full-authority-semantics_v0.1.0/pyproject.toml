[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aion-full-authority-semantics"
version = "0.1.0"
description = "Research-only authority delegation and authorization semantics prototype"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
pythonpath = ["src"]
