# Historical Branch and Release Records

This section preserves branch-disposition and repository-freeze snapshots. A snapshot may describe a prior branch count or pre-freeze state. It is not the current branch inventory and must not be rewritten to appear current.

The current branch model is documented by `docs/README.md` and `docs/RELEASE_STATUS.md`.
