# Selective-Promotion Canonical Evidence Guard — Vertical Slice

## Scope

This vertical slice is reconstructed on top of `main@e079fb7dfe7a04be7dcb94b8a059951a003caa94`. It uses the read-only CSOMI source only as provenance for the generic guard design; it does not import the CSOMI packet, literature, findings, fixtures, workflow or research conclusion.

## Flow

| Stage | Generic candidate artifact | Canonical purpose | Boundary |
|---|---|---|---|
| 1 | `CSOMI_CANONICAL_EVIDENCE_GUARD_RECORD_V0.1.0.json` | Represents a reusable guard record | It is a design guard, not an empirical or phenomenological claim. |
| 2 | `aion_canonical_evidence_invariants_v0.1.0.schema.json` | Requires category separation, access-grade fields, alternatives, disanalogies and falsifiers | It does not calculate consciousness, capability or scientific truth. |
| 3 | `check_canonical_evidence_invariants.py` | Fails closed on category collapse, CI-as-subjectivity evidence and omitted alternatives/disanalogies/falsifiers | It does not alter runtime, release or deployment behavior. |
| 4 | `test_canonical_evidence_invariants.py` | Exercises one positive generic record and negative invalid records | Passing tests are validation metadata only. |
| 5 | `aion-selective-promotion.yml` | Runs the guard validation on the candidate branch | The workflow is candidate-scoped and does not merge, release, deploy or apply Topics. |

## Reviewer assertions

The record’s `claim_type` is `RESEARCH_TOPIC` and its `claim_status` is `DESIGN_GUARD`. It cannot be silently reclassified as a `CAPABILITY` or `SCIENTIFIC_CONCLUSION`. The record includes explicit alternative explanations, cross-context disanalogies and falsifiers. The `EVID-CI-NON-EVIDENCE` channel is deliberately marked `NOT_EVIDENCE`, and `FALS-TEST-CI-SUBJECTIVITY` is `MACHINE_REJECT`.

The access-grade fields demonstrate a general provenance shape without importing any external source row. A canonical record may express `PRIMARY_FULLTEXT_DIRECTLY_VERIFIED`, `PRIMARY_ABSTRACT_DIRECTLY_VERIFIED`, `PRIMARY_METADATA_VERIFIED`, `PRIMARY_FULLTEXT_NOT_DIRECTLY_VERIFIED` or `AUTHORITATIVE_SECONDARY_CORROBORATED`, but must also retain access evidence. The grade does not become a scientific-truth field.

## Non-effects

This slice does not create a consciousness detector, change a model, add a runtime authority, collect live data, perform an experiment, declare subjectivity, declare consciousness, declare identity continuity, deploy, release, apply repository Topics or merge `main`. Its state is `CANONICAL_EFFECT=PENDING_OWNER_PROMOTION`, which means candidate review is possible but promotion has not occurred.
