# Disposable negative merge-control test

This file exists only on the disposable negative-test branch. The test intentionally omits a valid Main Transition Authority receipt so the Authority Gate remains HOLD/FAILURE while Quality is expected to pass. This branch and pull request must not be merged into `main`.
