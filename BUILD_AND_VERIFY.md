# Build and Verify

Use Python 3.11 or later. Run `python verify_public_package.py .` for manifest and policy checks. Each component may be tested from its directory with `python -m pytest`. This candidate contains no wheelhouse; dependency installation may require a separately governed environment. Do not execute unreviewed commands with elevated privileges.
