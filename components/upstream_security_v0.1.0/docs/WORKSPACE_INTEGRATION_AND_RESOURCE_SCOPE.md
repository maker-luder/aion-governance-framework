# Workspace integration and resource scope

This is a `LEVEL_2_LOCAL_MODULE_CHANGE`. Existing Governance Kernel, Episodic Core, Bazi Core, Engineering Workbench, Language Core and Identity/Lineage packages are unchanged; their accepted component evidence is reusable. Only this new component is revalidated.

The existing Identity/Lineage component remains authoritative for project identity, lineage and canonical writeback. This package contributes security signals and QA HOLD decisions through an integration boundary; it does not duplicate the canonical gate.

Success criteria are implementation, tests, offline install, package integrity and owner-review handoff. Non-blocking items include future source verification and live runtime integration. No next phase starts automatically.
