# Workspace inspection and integration

The workspace root is not a Git repository. Existing candidate locks include v1.5.0 Workbench, v1.6.0 Phase A/B, v1.7.0 gated Bazi, and Language Core v0.1.0. They retain `canonical_effect=NONE` or pending owner review and `subjectivity_conclusion=NOT_ESTABLISHED`.

This change is `LEVEL_2_LOCAL_MODULE_CHANGE`. Existing Kernel, Episodic, Bazi, Workbench, and Language Core verification evidence is `REUSABLE_EVIDENCE` because their source was not modified. Only this new component is revalidated. The Language Core model registry remains authoritative for its nodes; this component provides a capability adapter instead of a parallel model registry.
