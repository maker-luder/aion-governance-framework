# One-pass Convergence Workflow

Preflight → change set → dependency impact → minimum revalidation scope → implementation → affected validation → report → package → final self-check → Owner handoff → stop.

`TASK_START_COUNT_TARGET = 1`; `MAX_ENGINEERING_REWORK_ROUNDS = 1`.
