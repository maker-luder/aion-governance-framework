# External Input Provenance

External teacher/model responses are recorded as `EXTERNAL_MODEL_OR_TEACHER_INPUT`, with an actor ID and content hash. Adoption begins at `PENDING_OWNER_REVIEW`. It is never inferred to be an Owner statement and never becomes a patch automatically.
