# Candidate Workspace Security

Controls include relative-path validation, UNC/absolute/traversal rejection, link/junction checks, hard-link write rejection, UTF-8 and binary boundaries, size limits, atomic writes, pre-change snapshots, exact patch context, baseline hash comparison, diff, and rollback.

Historical archives and active baselines are never write targets.
