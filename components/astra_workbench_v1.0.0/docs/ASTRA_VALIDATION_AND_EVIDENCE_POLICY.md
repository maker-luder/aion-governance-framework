# Validation and Evidence Policy

Validation scope follows change impact. Reusable, invalidated, stale and non-reusable evidence are distinct states. Source hash, environment fingerprint and dependency scope determine reuse.

Document and packaging changes do not automatically trigger full engineering reruns. Runtime, dependency and security-policy changes justify broader validation. Every executed command becomes a hashed evidence reference.
