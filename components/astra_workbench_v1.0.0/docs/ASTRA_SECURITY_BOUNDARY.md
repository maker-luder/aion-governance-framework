# Security Boundary

The Workbench is local-only and candidate-only. It cannot promote, deploy, install services, modify active core, elevate privilege, upload data, contact external models, or submit review packets.

The optional localhost provider is loopback-only and disabled. Live local reasoning validation is `NOT_EXECUTED`.
